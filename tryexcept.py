#try block generate exception,because x is not defined...use try and except block
try:
    print(x)
except:
    print("an exception error by x")

#this statement raise an error ,x not defined... so need tryexcept block
print(x)